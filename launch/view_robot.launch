<?xml version="1.0"?>
<!--
  Used for visualising turtlebot in action.  
  It requires minimal.launch and optionally 3dsensor.launch to already be up 
  and running.
  -->
<launch>
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find assignment2)/rviz/robot.rviz" />
</launch>
