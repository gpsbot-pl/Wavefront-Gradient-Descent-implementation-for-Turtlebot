#ifndef __GRADIENT_DESCENT_HPP__
#define __GRADIENT_DESCENT_HPP__

#include <geometry_msgs/PoseStamped.h>

/**
 * 
 *
 * This method is calls the wavefront simulation each time a goal message is
 * received.
 */
void callback_simulation( const geometry_msgs::PoseStamped& msg_goal );

#endif
